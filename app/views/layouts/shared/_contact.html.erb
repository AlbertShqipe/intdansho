<%= form_with model: @contact, url: contacts_path, local: true do |form| %>
  <div class="inp">
    <%= form.text_field :name, required: true, placeholder: "Nom et Prénom*" %>
  </div>

  <div class="inp">
    <%= form.email_field :email, required: true, placeholder: "Email*" %>
  </div>

  <div class="inp">
    <%= form.text_area :message, required: true, placeholder: "Votre Message*" %>
  </div>

  <div class="inp-btn">
    <%= form.submit "Submit" %>
  </div>
<% end %>
